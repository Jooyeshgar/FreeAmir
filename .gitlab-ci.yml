stages:
  - test
  - deploy_develop

cache:
  paths:
    - vendor/
    - node_modules/

before_script:
  - cp .env.ci .env
  - cp docker-composeci.yml docker-compose.yml
  - chmod -R 777 storage/
  - chmod -R 777 bootstrap/cache/
  - echo "DB_PASSWORD=$DB_PASSWORD" >> .env
  - composer install --no-scripts --ignore-platform-reqs
  - chmod -R 777 vendor/
  - ./vendor/bin/sail up -d --force-recreate
  - ./vendor/bin/sail exec laravel.test git config --global --add safe.directory /var/www/html
  - ./vendor/bin/sail composer dump-autoload
  - ./vendor/bin/sail npm install --cache /var/www/html/.npm
  - ./vendor/bin/sail npm run build
  - ./vendor/bin/sail php artisan migrate:fresh
after_script:
  # - rm -f .env
  - ./vendor/bin/sail down

test:
  stage: test
  script:
    - ./vendor/bin/sail php artisan test --colors=never

deploy_develop:
  when: manual
  only:
    - main
  script:
    - ./vendor/bin/envoy run deploy
