<x-app-layout :title="__('cheques weighted_average_maturity')">
    <x-show-message-bags />
    <div class="space-y-4">
        <div class="flex justify-between"><div><h1 class="text-2xl font-bold">{{ __('cheques weighted_average_maturity') }}</h1><p class="opacity-70">{{ __('cheques weighted_average_maturity_help') }}</p></div><a class="btn btn-ghost btn-sm" href="{{ route('cheques.index') }}">{{ __('cheques back') }}</a></div>
        @isset($result)<div class="grid gap-3 sm:grid-cols-2 lg:grid-cols-4"><div class="stat bg-base-100 shadow rounded-box"><div class="stat-title">{{ __('cheques total_amount') }}</div><div class="stat-value text-xl">{{ formatNumber($result['total_amount']) }}</div></div><div class="stat bg-base-100 shadow rounded-box"><div class="stat-title">{{ __('cheques weighted_days') }}</div><div class="stat-value text-xl">{{ localizeNumber(number_format($result['weighted_days'], 2)) }}</div></div><div class="stat bg-base-100 shadow rounded-box"><div class="stat-title">{{ __('cheques average_due_date') }}</div><div class="stat-value text-xl">{{ formatDate($result['average_due_date']) }}</div></div><div class="stat bg-base-100 shadow rounded-box"><div class="stat-title">{{ __('cheques financial_cost') }}</div><div class="stat-value text-xl">{{ formatNumber($result['financial_cost']) }}</div></div></div>@endisset
        <form method="POST" action="{{ route('cheques.weighted-average-maturity.calculate') }}" class="card bg-base-100 shadow-xl">@csrf<div class="card-body"><div class="grid gap-3 md:grid-cols-3"><x-date-picker name="base_date" :value="old('base_date')" :title="__('cheques base_date')"/><label class="form-control"><span class="label-text mb-2">{{ __('cheques annual_rate') }}</span><input type="number" step="0.01" min="0" name="annual_rate" value="{{ old('annual_rate', 0) }}" class="input input-bordered"></label></div><div class="overflow-x-auto mt-4"><table class="table"><thead><tr><th></th><th>{{ __('cheques fields serial') }}</th><th>{{ __('cheques fields party') }}</th><th>{{ __('cheques fields amount') }}</th><th>{{ __('cheques fields due_date') }}</th></tr></thead><tbody>@foreach($cheques as $cheque)<tr><td><input class="checkbox checkbox-sm" type="checkbox" name="cheque_ids[]" value="{{ $cheque->id }}" @checked(in_array($cheque->id, old('cheque_ids', [])))></td><td>{{ localizeNumber($cheque->serial) }}</td><td>{{ $cheque->party?->name }}</td><td>{{ formatNumber($cheque->amount) }}</td><td>{{ formatDate($cheque->due_date) }}</td></tr>@endforeach</tbody></table></div><div class="card-actions justify-end"><button class="btn btn-primary">{{ __('cheques calculate') }}</button></div></div></form>
    </div>
</x-app-layout>
